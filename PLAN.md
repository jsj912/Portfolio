# Build plan

Superseded. This file originally held a plan invented here because the brief's own
phase list had not arrived (`DECISIONS.md` D-001).

The brief has since arrived in full. **Section 2 of `portfolio_build_prompt.md` is
the authoritative phase plan**, and section 7 is the authoritative quality gate.
That file is gitignored (D-004), so it is not in the repository — it lives in the
working directory only.

Phase status is tracked in the commit log; anything deferred is in `TODO.md` and
every judgement call is in `DECISIONS.md`.
