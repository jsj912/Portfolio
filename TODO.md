# TODO

Things deliberately left unset because the source material for them does not exist
yet. Per the brief: no invented facts, no placeholder text — anything unknown is
`null`, its UI is hidden, and it gets a line here.

## Blocking the build

- [ ] **Node.js runtime** — not installed on this machine; the Next.js scaffold
      cannot run until it is. You are installing it yourself (`DECISIONS.md`
      D-003a). Build resumes at Phase 1 once `node --version` answers.

## Missing from the brief

The brief arrived partially. Received: section 0 (ground rules) and section 1
(stack). Not yet received, and referenced by what was received:

- [ ] **Section 2** — never sent.
- [ ] **Section 3, the `CONTENT` block** — referenced by ground rule 1 as the single
      source for every factual claim on the site. Without it, no content-bearing
      component can be written at all: name, role, bio, projects, skills, links,
      dates, and education are all unknown.
- [ ] **Sections 4–7** — never sent (presumably pages/sections, design direction,
      components, checks).
- [ ] **Section 8** — referenced by section 1 for the Vercel deploy details.
